name: Link improved jornada styles

on:
  push:
    paths:
      - '.github/workflows/link-jornadas-css.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  link-css:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Link jornadas stylesheet
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('index.html')
          text = path.read_text(encoding='utf-8')
          if 'jornadas.css' not in text:
              text = text.replace('<link rel="stylesheet" href="matchdays.css" />', '<link rel="stylesheet" href="matchdays.css" />\n    <link rel="stylesheet" href="jornadas.css" />')
              path.write_text(text, encoding='utf-8')
          PY
      - name: Commit if needed
        run: |
          if ! git diff --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add index.html
            git commit -m "Load improved jornada stylesheet"
            git push
          fi
